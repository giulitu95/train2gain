<resources>
    <!-- Light application theme (default theme) -->
    <style name="app_theme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/theme_primary</item>
        <item name="android:colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="android:colorAccent">@color/theme_secondary</item>
        <item name="android:textColor">@color/theme_text</item>
        <item name="android:textColorHint">@color/theme_secondary</item>
        <item name="android:windowBackground">@color/theme_background</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <!-- Dark application theme. -->
    <style name="app_theme_dark" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/theme_primary</item>
        <item name="android:colorAccent">@color/theme_secondary</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>
</resources>
